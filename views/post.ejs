<%- include('partials/header', { title: post.title }) %>
<article class="post">
  <h1><%= post.title %></h1>
  <p class="meta">by <%= post.author %> on <%= post.created_at %></p>
  <div class="post-body"><%= post.body %></div>
  <% if (currentUser && currentUser.role === 'admin') { %>
    <form method="POST" action="/posts/<%= post.id %>/delete">
      <button type="submit">Delete post</button>
    </form>
  <% } %>
</article>
<section class="comments">
  <h2>Comments</h2>
  <% if (comments.length === 0) { %>
    <p>No comments yet.</p>
  <% } else { %>
    <% comments.forEach(function (comment) { %>
      <div class="comment">
        <p class="meta"><%= comment.author %> on <%= comment.created_at %></p>
        <%# [VULN-XSS-S] OWASP A03:2021 | CWE-79 | Report Insecure-3 | Issue #3 %>
        <%# WHY: the comment body is stored as given and printed raw, without escaping. A script %>
        <%#      payload stays in the database and runs for every later viewer. %>
        <p class="comment-body"><%- comment.body %></p>
      </div>
    <% }) %>
  <% } %>

  <% if (currentUser) { %>
    <form method="POST" action="/posts/<%= post.id %>/comments">
      <label for="body">Add a comment</label>
      <textarea id="body" name="body" required></textarea>
      <button type="submit">Comment</button>
    </form>
  <% } else { %>
    <p><a href="/login">Log in</a> to comment.</p>
  <% } %>
</section>

<p><a href="/">&larr; Back to posts</a></p>
<%- include('partials/footer') %>
